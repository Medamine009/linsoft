# Config minimale — port, Mongo, RabbitMQ, Eureka, Keycloak sont servis
# par le Config Server (config-service, port 8888).
spring:
  application:
    name: registration-service
  config:
    import: optional:configserver:http://localhost:8888
