# Config minimale — RabbitMQ, Mail, Eureka, Keycloak sont servis
# par le Config Server (config-service, port 8888).
spring:
  application:
    name: notification-service
  config:
    import: optional:configserver:http://localhost:8888
