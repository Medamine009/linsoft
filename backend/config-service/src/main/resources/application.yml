server:
  port: 8888

spring:
  application:
    name: config-service
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          # Les fichiers de config centralisés sont embarqués dans le jar (classpath:/config)
          search-locations: classpath:/config
