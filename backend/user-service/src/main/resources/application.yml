# Config minimale — tout le reste (port, datasource, eureka, keycloak)
# est servi par le Config Server (config-service, port 8888).
spring:
  application:
    name: user-service
  config:
    import: optional:configserver:http://localhost:8888
